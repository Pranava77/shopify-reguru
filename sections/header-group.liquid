{%- liquid
  assign bg_image = section.settings.background_image
-%}

{%- comment -%}
  Header Group Section
  Contains the announcement bar, main header navigation, and category navigation
{%- endcomment -%}

{%- for block in section.blocks -%}
  {%- case block.type -%}
    {%- when '@app' -%}
      {% render block %}
  {%- endcase -%}
{%- endfor -%}

{% schema %}
{
  "name": "Header Group",
  "settings": [
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background Image"
    }
  ]
}
{% endschema %}
