{%- liquid
  assign heading = section.settings.heading | default: 'FAQs'
  assign bg_image = section.settings.background_image
-%}

<section class="faq"{% if bg_image != blank %} style="background-image: url('{{ bg_image | image_url: width: 1920 }}'); background-size: cover; background-position: center; background-repeat: no-repeat;"{% endif %}>
  <div class="faq__container">
    <div class="faq__card card--lg">
      <div class="faq__header">
        <h2 class="faq__title">{{ heading }}</h2>
        <div class="faq__decoration"></div>
      </div>

      <div class="faq__tabs">
        {%- for block in section.blocks -%}
          {%- if block.type == 'tab' -%}
            <button 
              type="button" 
              class="faq__tab tab--pill{% if forloop.first %} active{% endif %}"
              data-tab-id="{{ block.id }}"
              {{ block.shopify_attributes }}
            >
              {{ block.settings.label }}
            </button>
          {%- endif -%}
        {%- endfor -%}

        {%- if section.blocks.size == 0 -%}
          <button type="button" class="faq__tab tab--pill active" data-tab-id="general">General</button>
          <button type="button" class="faq__tab tab--pill" data-tab-id="services">services</button>
          <button type="button" class="faq__tab tab--pill" data-tab-id="business">Business</button>
        {%- endif -%}
      </div>

      <div class="faq__content">
        {%- assign faq_index = 0 -%}
        {%- assign current_tab_id = blank -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'tab' -%}
            {%- assign current_tab_id = block.id -%}
          {%- elsif block.type == 'faq_item' -%}
            {%- assign faq_index = faq_index | plus: 1 -%}
            <div data-tab-id="{{ current_tab_id | default: 'general' }}">
              {%- render 'faq-item',
                index: faq_index,
                question: block.settings.question,
                answer: block.settings.answer
              -%}
            </div>
          {%- endif -%}
        {%- endfor -%}

        {%- if section.blocks.size == 0 -%}
          {%- assign default_faqs = 'Do ReGuru products come with warranty?|What is the return policy for ReGuru products?|Do ReGuru products come with warranty?' | split: '|' -%}
          {%- for faq in default_faqs -%}
            <div data-tab-id="general">
              <details class="faq__item">
                <summary class="faq__question">
                  <span class="faq__number">0{{ forloop.index }}</span>
                  <span class="faq__question-text">{{ faq }}</span>
                  <span class="faq__icon">
                    <svg width="41" height="43" viewBox="0 0 41 43" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M10 17L20.5 27L31 17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </span>
                </summary>
                <div class="faq__answer">
                  <p>Yes, all ReGuru products come with a comprehensive warranty. Our refurbished electronics are thoroughly tested and certified, ensuring quality and reliability. Specific warranty terms vary by product category.</p>
                </div>
              </details>
            </div>
          {%- endfor -%}
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

{% stylesheet %}
.faq {
  padding: 60px 0;
}

.faq__container {
  max-width: var(--container-max);
  margin: 0 auto;
  padding: 0 var(--container-padding);
  display: flex;
  justify-content: center;
}

.faq__card {
  width: 100%;
  max-width: 977px;
  padding: 32px 40px 48px;
  background-color: var(--color-white);
  border: 2px solid var(--color-black);
  border-radius: 12px;
  box-shadow: 5px 4px 0px 0px var(--color-black);
  animation: faqCardSlideIn 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
  opacity: 0;
  transform: translateY(20px);
}

.faq__header {
  text-align: center;
  margin-bottom: 24px;
}

.faq__title {
  font-family: var(--font-body);
  font-weight: 400;
  font-size: 47px;
  line-height: 1.31;
  color: var(--color-black);
  margin: 0 0 16px;
}

.faq__decoration {
  width: 273px;
  height: 2px;
  margin: 0 auto;
  background: linear-gradient(90deg, transparent 0%, var(--color-black) 50%, transparent 100%);
}

.faq__tabs {
  display: flex;
  justify-content: center;
  gap: 24px;
  margin-bottom: 40px;
}

.faq__tab {
  padding: 8px 21px;
  font-family: var(--font-heading);
  font-weight: 400;
  font-size: 18px;
  color: var(--color-black);
  background-color: var(--color-white);
  border: 0.5px solid var(--color-black);
  border-radius: var(--radius-pill);
  box-shadow: 3px 3px 0px 0px var(--color-black);
  cursor: pointer;
  transition: background-color var(--transition-fast), transform var(--transition-fast), box-shadow var(--transition-fast);
}

.faq__tab:hover {
  background-color: var(--color-light-blue);
  transform: translate(-1px, -1px);
  box-shadow: 4px 4px 0px 0px var(--color-black);
}

.faq__tab:active {
  transform: translate(1px, 1px);
  box-shadow: 2px 2px 0px 0px var(--color-black);
}

.faq__tab.active {
  background-color: var(--color-blue);
  color: var(--color-white);
  animation: tabActivate 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
}

@keyframes tabActivate {
  0% {
    transform: scale(0.95);
  }
  50% {
    transform: scale(1.05);
  }
  100% {
    transform: scale(1);
  }
}

.faq__content {
  max-width: 602px;
  margin: 0 auto;
}

/* Animation for FAQ items when tab changes */
.faq__content[data-animating] [data-tab-id] {
  animation: faqItemSlideIn 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
  opacity: 0;
  transform: translateX(-10px);
}

.faq__content[data-animating] [data-tab-id]:nth-child(1) {
  animation-delay: 0.05s;
}

.faq__content[data-animating] [data-tab-id]:nth-child(2) {
  animation-delay: 0.1s;
}

.faq__content[data-animating] [data-tab-id]:nth-child(3) {
  animation-delay: 0.15s;
}

.faq__content[data-animating] [data-tab-id]:nth-child(4) {
  animation-delay: 0.2s;
}

.faq__content[data-animating] [data-tab-id]:nth-child(5) {
  animation-delay: 0.25s;
}

.faq__content[data-animating] [data-tab-id]:nth-child(6) {
  animation-delay: 0.3s;
}

.faq__content[data-animating] [data-tab-id]:nth-child(7) {
  animation-delay: 0.35s;
}

.faq__content[data-animating] [data-tab-id]:nth-child(8) {
  animation-delay: 0.4s;
}

/* Keyframe animations */
@keyframes faqCardSlideIn {
  0% {
    opacity: 0;
    transform: translateY(20px) scale(0.98);
    box-shadow: 2px 2px 0px 0px var(--color-black);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
    box-shadow: 5px 4px 0px 0px var(--color-black);
  }
}

@keyframes faqItemSlideIn {
  0% {
    opacity: 0;
    transform: translateX(-10px);
  }
  100% {
    opacity: 1;
    transform: translateX(0);
  }
}

/* FAQ Item Styles */
.faq__item {
  border-bottom: 1px solid rgba(0, 0, 0, 0.2);
  padding: 14px 0;
  transition: transform var(--transition-fast);
}

@media (hover: hover) and (pointer: fine) {
  .faq__item:hover {
    transform: translateX(3px);
  }
}

.faq__item:last-child {
  border-bottom: none;
}

.faq__question {
  display: flex;
  align-items: center;
  gap: 13px;
  cursor: pointer;
  list-style: none;
}

.faq__question::-webkit-details-marker {
  display: none;
}

.faq__number {
  font-family: var(--font-body);
  font-weight: 400;
  font-size: 30px;
  line-height: 1.31;
  color: var(--color-black);
}

.faq__question-text {
  flex: 1;
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 24px;
  line-height: 1.31;
  color: var(--color-black);
}

.faq__icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 41px;
  height: 43px;
  transition: transform var(--transition-base);
}

.faq__item[open] .faq__icon {
  transform: rotate(180deg);
}

.faq__question {
  transition: transform var(--transition-fast);
}

.faq__item[open] .faq__question {
  transform: translateX(2px);
}

.faq__icon svg {
  width: 100%;
  height: 100%;
}

.faq__answer {
  padding: 16px 0 8px 43px;
  font-family: var(--font-body);
  font-size: 16px;
  line-height: 1.6;
  color: var(--color-text-secondary);
}

.faq__item[open] .faq__answer {
  animation: faqAnswerSlideDown 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
}

@keyframes faqAnswerSlideDown {
  0% {
    opacity: 0;
    transform: translateY(-10px);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

.faq__answer p {
  margin: 0;
}

@media screen and (max-width: 768px) {
  .faq {
    padding: 40px 0;
  }

  .faq__card {
    padding: 24px 20px 32px;
    max-width: 100%;
    box-shadow: 3px 3px 0px 0px var(--color-black);
    animation: faqCardSlideInMobile 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
    transform: translateY(10px);
  }

  .faq__title {
    font-size: 32px;
  }

  .faq__decoration {
    width: 200px;
  }

  .faq__tabs {
    flex-wrap: wrap;
    gap: 12px;
    justify-content: center;
  }

  .faq__tab {
    font-size: 14px;
    padding: 6px 16px;
    box-shadow: 2px 2px 0px 0px var(--color-black);
  }

  .faq__tab:hover {
    transform: none;
    box-shadow: 2px 2px 0px 0px var(--color-black);
  }

  .faq__content {
    max-width: 100%;
  }

  .faq__question {
    gap: 10px;
    flex-wrap: wrap;
  }

  .faq__number {
    font-size: 22px;
    flex-shrink: 0;
  }

  .faq__question-text {
    font-size: 18px;
    min-width: 0;
    word-wrap: break-word;
  }

  .faq__icon {
    width: 32px;
    height: 34px;
    flex-shrink: 0;
  }


  .faq__item[open] .faq__question {
    transform: none;
  }

  .faq__answer {
    padding-left: 32px;
    font-size: 14px;
  }

  /* Reduce animation intensity on mobile */
  .faq__content[data-animating] [data-tab-id] {
    animation: faqItemSlideInMobile 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
    transform: translateX(-5px);
  }
}

@keyframes faqItemSlideInMobile {
  0% {
    opacity: 0;
    transform: translateX(-5px);
  }
  100% {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes faqCardSlideInMobile {
  0% {
    opacity: 0;
    transform: translateY(10px) scale(0.99);
    box-shadow: 2px 2px 0px 0px var(--color-black);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
    box-shadow: 3px 3px 0px 0px var(--color-black);
  }
}

@media screen and (max-width: 480px) {
  .faq {
    padding: 30px 0;
  }

  .faq__card {
    padding: 20px 16px 24px;
    border-width: 1.5px;
    box-shadow: 2px 2px 0px 0px var(--color-black);
  }

  .faq__title {
    font-size: 28px;
    margin-bottom: 12px;
  }

  .faq__decoration {
    width: 150px;
  }

  .faq__tabs {
    gap: 8px;
    margin-bottom: 24px;
  }

  .faq__tab {
    font-size: 12px;
    padding: 5px 12px;
  }

  .faq__number {
    font-size: 20px;
  }

  .faq__question-text {
    font-size: 16px;
  }

  .faq__icon {
    width: 28px;
    height: 30px;
  }

  .faq__answer {
    padding-left: 28px;
    font-size: 13px;
    padding-top: 12px;
  }

  .faq__item {
    padding: 12px 0;
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "FAQ",
  "tag": "section",
  "class": "faq-section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "FAQs"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background Image"
    }
  ],
  "blocks": [
    {
      "type": "tab",
      "name": "Category Tab",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "Tab Label",
          "default": "General"
        }
      ]
    },
    {
      "type": "faq_item",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "Do ReGuru products come with warranty?"
        },
        {
          "type": "textarea",
          "id": "answer",
          "label": "Answer",
          "default": "Yes, all ReGuru products come with a comprehensive warranty. Our refurbished electronics are thoroughly tested and certified, ensuring quality and reliability."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "FAQ",
      "blocks": [
        {
          "type": "tab",
          "settings": {
            "label": "General"
          }
        },
        {
          "type": "tab",
          "settings": {
            "label": "Services"
          }
        },
        {
          "type": "tab",
          "settings": {
            "label": "Business"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "Do ReGuru products come with warranty?",
            "answer": "Yes, all ReGuru products come with a comprehensive warranty. Our refurbished electronics are thoroughly tested and certified, ensuring quality and reliability."
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "Do ReGuru products come with warranty?",
            "answer": "Yes, all ReGuru products come with a comprehensive warranty. Our refurbished electronics are thoroughly tested and certified, ensuring quality and reliability."
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "Do ReGuru products come with warranty?",
            "answer": "Yes, all ReGuru products come with a comprehensive warranty. Our refurbished electronics are thoroughly tested and certified, ensuring quality and reliability."
          }
        }
      ]
    }
  ]
}
{% endschema %}
